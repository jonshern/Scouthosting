node_modules/
.env
.env.local
.DS_Store
*:Zone.Identifier
*.log
npm-debug.log*
.vscode/
.idea/

# Prisma
prisma/migrations/dev.db*

# Local uploads (per-org photo storage)
var/

# Per-machine docker-compose overrides (e.g. remap host port when 5432
# is already taken). Each developer keeps their own.
docker-compose.override.yml
